# trading_bot.1001

**Core language: Ruby** — treat new application code, tooling, and Cursor guidance as Ruby-first (Rails 8 conventions where applicable). The Alpaca momentum loop under `bot.py` and `config/` remains a **legacy Python** runner until migrated.

See `CLAUDE_PROJECT_INSTRUCTIONS.md` for architecture and setup.
