# frozen_string_literal: true

source "https://rubygems.org"

# Primary language for this repository is Ruby. Add Rails/app gems here as the
# Ruby surface grows. The live trading loop is still Python (see requirements.txt).

ruby ">= 3.2.0"
